/// AtlasKit — the single API client + models for Atlas's Apple apps.
/// Pure Foundation (+ Security for Keychain); compiles for iOS **and** watchOS
/// unchanged so the Phase-3 watch target reuses it wholesale. No SwiftUI/UIKit here.
public enum AtlasKit {
    public static let version = "0.1.0"
}
